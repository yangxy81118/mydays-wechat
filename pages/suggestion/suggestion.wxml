<!--index.wxml-->
<view class="header">
  <view class="words">
      <text class="title">{{day.name}}</text>
      <text class="small date">{{day.year}}年{{day.month}}月{{day.date}}日</text>
      <text class="small remain">还有{{day.remain}}天</text>
  </view>
  <view class="bk">
      <image mode="aspectFill" src="{{day.image}}" />
  </view>
</view>
<view wx:if="{{day.remain > 7}}" class="comments">
  还有一段时间，可以提早构思一下哦~
</view>
<view wx:else class="comments">
  就快要到了，尽快准备好哦，以免留下遗憾~
</view>
<scroll-view scroll-y class="suggestions">
  <view class="sug-box" wx:for="{{sugs}}" wx:for-index="idx" wx:for-item="item" >
      <view class="img">
          <image  src="{{item.image}}" />
      </view>
      <view class="word-box">
         <text>{{item.content}}</text>
      </view>
  </view>
</scroll-view>



